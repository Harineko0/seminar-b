[project]
name = "wasm_cv"
version = "0.1.0"
description = "wasm_cv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "hypothesis>=6.150.0",
    "pytest>=9.0.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["pbt.py"]

[tool.uv]
managed = true
