[project]
name = "wasm_cv"
version = "0.1.0"
description = "wasm_cv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "crosshair-tool>=0.0.101",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = []

[tool.uv]
managed = true
