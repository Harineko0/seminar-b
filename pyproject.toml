[project]
name = "tinyproto-research"
version = "0.1.0"
description = "Researching AI Coding Agents with Symbolic Execution and PBT"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test.py"]

[tool.uv]
managed = true
